/** Error code when force-approve is requested without reviewer conflict. */
export const CANNOT_FORCE_APPROVE_WITHOUT_CONFLICT_ERROR_CODE = 'SRVREV0001' as const;

/** Error code when survey objects and audits are requested without a corrected response. */
export const CORRECTED_RESPONSE_REQUIRED_ERROR_CODE = 'SRVREV0002' as const;
